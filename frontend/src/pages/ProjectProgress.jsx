import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useUser } from "../context/UserContext";

function ProjectProgress() {
  return <h1>Progress Progress</h1>;
}

export default ProjectProgress;
